apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding

metadata:
  name: aiops-cluster-reader-binding

subjects:
  - kind: ServiceAccount 
    name: aiops-agent
    namespace: default

roleRef:
  kind: ClusterRole
  name: aiops-cluster-reader
  apiGroup: rbac.authorization.k8s.io
